/**
 * 语音 → 发语音（Hold to Talk）子页的 Mock 示例文本（PRD §6.3 / §10.6）。
 *
 * 对应后端需求：B-2（语音链路：录音 → ASR → 文本，本期未对接）。
 * B 类 mock 数据——界面必须带「原型 · 功能开发中」标识（voice-talk 顶部
 * 原型横幅）。真实发送链路保留（hold-to-talk 确实会发消息），仅内容为占位。
 */
export const VOICE_TALK_MOCK_CONTENT = "（语音原型）请稍后补充需求描述";
